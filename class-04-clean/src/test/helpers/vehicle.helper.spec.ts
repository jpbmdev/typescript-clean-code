import { VehicleHelper } from "../../helpers/vehicle.helper";

describe("Food Helper", () => {
  test("Food Helper", () => {
    const vehicleHelper = new VehicleHelper();
    expect(vehicleHelper).toEqual(vehicleHelper);
  });
});
